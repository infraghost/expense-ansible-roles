# - name: Install Few Packages
#   hosts: all
#   become: true
#   tasks:
#     - name: Install nano Editor
#       ansible.builtin.dnf:
#         name: nano
#         state: present
    
#     - name: Install ansible
#       ansible.builtin.dnf:
#         name: ansible
#         state: present

- name: Install Packages
  hosts: all
  become: yes
  tasks:
  - name: Install nano editor
    ansible.builtin.dnf:
      name: nano
      state: present
  
  - name: Install Ansible
    ansible.builtin.dnf:
      name: ansible
      state: present
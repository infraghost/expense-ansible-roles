- name: Install Packages
  hosts: all
  become: yes
  tasks:
  - name: Install nano editor
    ansible.builtin.dnf:
      name: nano
      state: present
  
  - name: Install Ansible
    ansible.builtin.dnf:
      name: ansible
      state: present